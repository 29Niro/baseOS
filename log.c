#include "log.h"
//#include "fb.h"
#include "common/common.h"
#include "serial_port.h"
//#include <timer.h>

/*char message[] = "baseOS";


void print_screen(s8int buffer[]) {
 write(buffer, strlen(buffer)); 
}

void print_serial(s8int buffer[]) {
  serial_write(SERIAL_COM1_BASE, buffer, strlen(buffer));
}

void print_screen_ch(s8int ch) {
 write(&ch, 1); 
}

void print_serial_ch(s8int ch) {
 serial_write(SERIAL_COM1_BASE, &ch, 1); 
}

void clear_screen() {
 fb_clear(); 
}*/

/** init_display:
 *  Reset screen and display splash screen
 */
/*void init_display() {
  fb_clear();
  write(message, 20);
}*/
